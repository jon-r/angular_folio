import { FolioListDirective } from './folio-list.directive';

describe('FolioListDirective', () => {
  it('should create an instance', () => {
    const directive = new FolioListDirective();
    expect(directive).toBeTruthy();
  });
});
