import { Injectable } from '@angular/core';
import { Subject }    from 'rxjs/Subject';

import { Grid } from './grid'

@Injectable()
export class GridService {

/*
  private gridSource = new Subject<Grid>;

  grid$  = this.gridSource.asObservable();

  set(window): void {
    let width = window.target.innerWidth;
    let height = window.target.innerHeight;

    let squareDims = 20;
    let xCount = Math.floor(width / squareDims)
    let yCount = Math.floor(height / squareDims)

//    this.gridSource = {
//      squareDims : squareDims,
//      xCount : xCount,
//      yCount : yCount
//    };
    this.gridSource.
  };
*/


//  setPos(x: number,y: number) {
//   // let obj = ;
//    let outStr = 'translate(';
//
//    for (let i in arr[x,y]) {
//      let n = (arr[i] > 0) ? arr[i] : this.grid[i + 'count'] - arr[i]
//      n = n * this.grid.squareDims;
//
//    }
//
//  };



}


